node_modules/
dist/
.DS_Store
server/data/

# local-only Claude Code settings (worktree base, permissions)
.claude/settings.local.json

# agent worktrees live here; they are checkouts, not content
.claude/worktrees/

# the design canvas is seeded from design/*.dc.html and published; the seeded page is a
# two-megabyte build artefact and belongs in the repository no more than dist/ does
ai-world-interface.html
collision-report.txt
wire-report.txt
halves-report.txt
economy-report.txt
tick-report.txt
playtest-report.txt

# throwaway scripts for looking at the game — screenshots, one-off probes
.scratch/
unreached-report.txt

# the note a killed `chore fallbacks` leaves behind, so the next run knows to tidy up
.fallbacks-in-flight
