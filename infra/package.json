{
  "name": "ai.world-infra",
  "private": true,
  "description": "The home server, as code. Kept out of the game's package.json so Pulumi never ships to a browser.",
  "type": "module",
  "scripts": {
    "preview": "pulumi preview",
    "up": "pulumi up",
    "refresh": "pulumi refresh",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@pulumi/pulumi": "^3.140.0",
    "@pulumi/command": "^1.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
