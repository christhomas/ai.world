name: ai_world_flutter
description: Native-texture Flutter client for AI World.
publish_to: none
version: 0.92.6+1

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
